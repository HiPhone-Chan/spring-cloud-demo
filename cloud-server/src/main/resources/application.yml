server:
  port: 8761
spring:
  main:
    banner-mode: 'off'
  application:
    name: cloud-server
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
logging:
  level:
    root: info
    com.netflix.eureka: OFF
    com.netflix.discovery: OFF